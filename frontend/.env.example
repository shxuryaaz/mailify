# Backend base URL
VITE_API_BASE=http://localhost:8000

# VAPID public key (from `python -m app.scripts.gen_keys`) — the service worker
# needs it to subscribe to push. Must match the backend's VAPID_PUBLIC_KEY.
VITE_VAPID_PUBLIC_KEY=
